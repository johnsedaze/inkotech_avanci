# Техническое задание: обмен распределениями оплат ERP → БП по HTTP

**Версия:** 1.5  
**Дата:** 06.04.2026  

**Связанные материалы:**

- `Идея ТЗ по обмену.md` — исходные требования
- `метаданные.md` — метаданные конфигурации ERP (Управление предприятием); после доработки по п. 3.7 актуализировать описание регистра `ИНК_РаспределениеОплатПоЭтапам`
- `ПримерСоединенияСБП.md` — **действующий пример** HTTP-вызова БП из ERP: выборка `ПодключениеHTTP` / `СерверHTTP` по организации через ИНН, `HTTPСоединение`, разбор ответа
- `http/metadata_bp.md` — метаданные конфигурации БП
- `http/ИНК_Бухгалтерия.xml` — описание HTTP-сервиса в БП
- `http/http_module.bsl` — текущий модуль HTTP-сервиса (эталон вызова/ответа)

---

## 1. Цель и границы задачи

### 1.1. Цель

Обеспечить перенос зафиксированных строк распределения оплат из регистра сведений ERP `ИНК_РаспределениеОплатПоЭтапам` в учётные документы базы бухгалтерии (БП) через **HTTP-сервис 1С**, с обратной фиксацией результата в ERP (признак `ВыгруженоВБП`).

### 1.2. Участники

| Роль | Конфигурация | Роль в обмене |
|------|----------------|---------------|
| Источник данных | ERP: Управление предприятием | Отбор строк, вызов сервиса БП, установка `ВыгруженоВБП` |
| Приёмник | БП (типовая бухгалтерия, см. `metadata_bp.md`) | Поиск документов по публичным идентификаторам, запись связей «оплата ↔ реализация», ответ ERP |

### 1.3. Вне скоупа (если не согласовано отдельно)

- **Первичная синхронизация** документов ERP→БП в объёме **данного ТЗ не требуется** (отдельная постановка и разработка под неё не входят в задачу обмена). Для работы сервиса в БП по-прежнему должны находиться документы оплаты и реализации по УИД в регистре **`ПубличныеИдентификаторыСинхронизируемыхОбъектов`** — это **исходная предпосылка** среды, а не результат выполнения настоящего ТЗ.
- **Постановка и реализация распределения оплат в ERP**, заполнение и пересчёт регистра **`ИНК_РаспределениеОплатПоЭтапам`** любыми иными ТЗ и модулями. Настоящий документ **ни с чем из этого не связан** и описывает **только обмен**: отбор уже существующих строк, HTTP, разбор ответа, признак **`ВыгруженоВБП`** и диагностику обмена (п. 3.7, п. 6).
- Выгрузка строк без документа **Реализация** (см. п. 4.3).

---

## 2. Термины

- **Строка распределения** — одна запись регистра сведений `ИНК_РаспределениеОплатПоЭтапам` с полным набором измерений (ключ записи).
- **УИД документа ERP** — `УникальныйИдентификатор()` ссылки на документ в ERP, строкой (36 символов), в том виде, в каком он хранится в `ПубличныеИдентификаторыСинхронизируемыхОбъектов.Идентификатор` в БП.
- **ПДС (ERP)** — документ `ПоступлениеБезналичныхДенежныхСредств`.
- **Поступление на р/с (БП)** — документ `ПоступлениеНаРасчетныйСчет` (аналог оплаты безналом в БП по составу обмена).
- **РТУ (ERP)** — `РеализацияТоваровУслуг`.
- **Реализация (БП)** — `РеализацияТоваровУслуг`.
- **База БП (целевая)** — информационная база бухгалтерии, в которую отправляется HTTP-запрос; набор таких баз задаётся регистром `ИНК_ПараметрыПодключенийКБазамДанных`.
- **Ключ маршрутизации** — ИНН организации заказа клиента, по которому выбирается строка регистра параметров подключения (и далее URL HTTP).
- **УИД узла УФ-обмена (для БП)** — строковый **`Код`** узла плана обмена **`СинхронизацияДанныхЧерезУниверсальныйФормат`**, взятого для **текущей** информационной базы ERP (узел с признаком «этот узел»). Тот же символьный идентификатор в БП хранится у **другого** узла того же плана (не «этот узел») и используется как **`УзелИнформационнойБазы`** при отборе из `ПубличныеИдентификаторыСинхронизируемыхОбъектов`. *Имя плана в метаданных ERP/БП должно совпадать с используемым при настройке синхронизации через универсальный формат (в выгрузке БП фигурирует `ПланОбмена.СинхронизацияДанныхЧерезУниверсальныйФормат`).*

---

## 3. Источник данных в ERP

### 3.1. Регистр сведений `ИНК_РаспределениеОплатПоЭтапам`

Структура по `метаданные.md`:

**Измерения:**

- `ЗаказКлиента` — Документ.ЗаказКлиента  
- `Реализация` — Документ.РеализацияТоваровУслуг  
- `ДокументОплаты` — Документ.ПоступлениеБезналичныхДенежныхСредств  
- `УИДЭтапа` — Строка  
- `Зафиксировано` — Булево  

**Ресурсы:**

- `Сумма` — Число  
- `ТипОперации` — Перечисление.ИНК_ТипыОпераций (`Аванс`, `ЗачетОплаты`)  
- `ЭтоПереизбыток` — Булево  

**Реквизиты:**

- `ВыгруженоВБП` — Булево  
- Реквизиты **диагностики обмена** — по п. 3.7 (добавляются в метаданных в рамках данного ТЗ).

### 3.2. Условие отбора строк к выгрузке

В пакет обмена попадают записи, для которых одновременно:

1. `Зафиксировано = Истина`  
2. `ВыгруженоВБП = Ложь`  
3. **Дополнительно (обязательное уточнение для реализации):** `Реализация` заполнена (не пустая ссылка).  
   - *Обоснование:* в БП по описанию задачи связь оформляется между **поступлением на р/с** и **реализацией**; строки «ожидания реализации» без `Реализация` в ERP не имеют пары документов в БП в смысле п. 4.2 идеи.

При необходимости выгрузки иных случаев (например, только аванс без РТУ) — отдельное согласование и доработка ТЗ.

### 3.7. Диагностика обмена: реквизиты в регистре `ИНК_РаспределениеОплатПоЭтапам`

**Задача:** при `ВыгруженоВБП = Ложь` фиксировать **последнюю** попытку обмена и причину отказа (код из ответа БП, сеть, отсутствие маршрута и т.д.), не раздувая модель отдельным регистром «журнал обмена», если не требуется **полная история** всех попыток.

**Рекомендация ТЗ:** добавить в регистр **`ИНК_РаспределениеОплатПоЭтапам`** реквизиты (не измерения):

| Имя (проект) | Тип | Назначение |
|--------------|-----|------------|
| `ДатаВремяПоследнейПопыткиВыгрузкиВБП` | Дата | Момент последней попытки отправки строки (успех или ошибка). |
| `КодРезультатаПоследнейВыгрузкиВБП` | Строка (короткая, напр. 20) | Код из контракта: `OK`, `NOT_FOUND`, `LOCKED`, `CONFIG_ERROR`, `HTTP_ERROR`, пусто до первой попытки. |
| `СообщениеПоследнейВыгрузкиВБП` | Строка (длина по согласованию, напр. 0 = неограниченно или 500) | Текст из `message` ответа БП или краткая техническая причина на стороне ERP. |

**Правила заполнения:**

- При **`status = OK`:** `ВыгруженоВБП = Истина`; обновить **`ДатаВремяПоследнейПопыткиВыгрузкиВБП`**, **`КодРезультатаПоследнейВыгрузкиВБП` = `OK`**; **`СообщениеПоследнейВыгрузкиВБП`** очистить или заполнить нейтральной строкой (единый подход в коде).  
- При **любой неуспешной** попытке по строке: **`ВыгруженоВБП` не менять**; обновить все три реквизита диагностики по фактическому результату.  
- Если строка **не попадала** в HTTP-пакет (например, нет маршрута по ИНН до вызова БП): записать **`CONFIG_ERROR`** и пояснение в сообщении на уровне **той же записи** регистра.

**Отдельный регистр сведений** для журнала обмена **не требуется**, если достаточно **последнего** состояния по строке. Вводить второй регистр имеет смысл только при требовании **аудита всех попыток** во времени (несколько записей на одну строку распределения) — это вне минимального объёма настоящего ТЗ.

---

### 3.3. Порядок обработки

- Рекомендуемый порядок сортировки внутри **одной целевой базы БП**: `(ДокументОплаты.Дата, ДокументОплаты.Номер, ДокументОплаты)`, затем `(Реализация.Дата, Реализация.Номер)`, затем `УИДЭтапа` — для предсказуемости повторных запусков.
- При нескольких базах сначала группировка по **ИНН организации** (см. п. 3.4), внутри группы — сортировка как выше.

### 3.4. Определение базы БП: регистр `ИНК_ПараметрыПодключенийКБазамДанных` и ИНН организации

Параметры подключения к базам бухгалтерии **не задаются константой**: они хранятся в регистре сведений **`ИНК_ПараметрыПодключенийКБазамДанных`** (`метаданные.md`).

**Структура регистра (используемые поля):**

| Элемент | Назначение |
|---------|------------|
| Измерение `ИмяБазы` | Логическое имя записи (идентификатор строки настройки). |
| Ресурс `ИНН` | ИНН организации, которой соответствует данная база БП. |
| Ресурс `ПодключениеHTTP` | **Префикс пути** публикации HTTP-сервиса на веб-сервере **без** имени конкретного шаблона URL (в коде к нему добавляется `"/<ИмяШаблона>"` и при необходимости query-string). Пример из внедрения: префикс вида `/INK_SYNC/hs/Buh`, итоговый путь к ведомостям: префикс + `"/SalaryStatement"`. |
| Ресурс `СерверHTTP` | **Имя хоста** (без схемы `http://`) для конструктора `HTTPСоединение`, например `win-sql-dev.inkotech.ru`. |
| Ресурсы `СтрокаПодключения`, `Сервер`, `БазаДанных`, `Пользователь` | Используются при **интеграциях не по HTTP** (COM и т.п.); для обмена по HTTP-сервису опорными считаются **`ПодключениеHTTP` / `СерверHTTP`**, если иное не зафиксировано в проекте. |

**Правило выбора базы для строки распределения:**

1. По измерению **`ЗаказКлиента`** строки регистра `ИНК_РаспределениеОплатПоЭтапам` получить **`Организацию`** заказа (реквизит документа `ЗаказКлиента` в ERP).  
2. Получить пару **`ПодключениеHTTP`**, **`СерверHTTP`** из регистра `ИНК_ПараметрыПодключенийКБазамДанных` по совпадению **`регистр.ИНН = Справочник.Организации.ИНН`** при отборе **`Организации.Ссылка = &Организация`** — **как в** `ПримерСоединенияСБП.md` (запрос с `ВНУТРЕННЕЕ СОЕДИНЕНИЕ` на `Справочник.Организации`). Эквивалентно: сначала ИНН организации пакетно, затем поиск строки регистра по ИНН (п. 3.5 при массовой выгрузке удобно один запрос по множеству организаций).  
3. Сравнение ИНН: при необходимости нормализация (пробелы, 10/12 знаков) — если в данных возможен разнобой.  
4. Сборка вызова: **`СерверHTTP`** — первый аргумент **`HTTPСоединение`**; путь запроса — **`ПодключениеHTTP` + "/" + ИмяШаблонаURL** и при необходимости query-string (см. п. 5.6).

**Условие пригодности (как в примере):** оба поля **`ПодключениеHTTP`** и **`СерверHTTP`** непустые; иначе вызов не выполнять — ошибка маршрутизации.

**Если для организации записи в регистре нет:** строки распределения с такой организацией **не отправлять** в БП; зафиксировать ошибку маршрутизации (`CONFIG_ERROR` / аналог), **`ВыгруженоВБП` не менять**. Поведение при **нескольких** строках регистра с одним и тем же `ИНН` — **открытый вопрос 9** (запрет дублей на уровне данных или приоритет по `ИмяБазы`).

### 3.5. Несколько организаций и несколько баз БП: стратегия соединений

В одной выборке к выгрузке строки могут относиться к **разным организациям**, значит — к **разным URL** баз БП. Требуется явно выбрать и закрепить в реализации одну из стратегий (либо комбинацию).

**Вариант A — группировка без «прогрева» (рекомендуется по умолчанию для HTTP):**

1. После отбора строк по п. 3.2 дополнить каждую строку вычисленным **`ИНН`** и ссылкой на параметры HTTP (или ключом `ИмяБазы`).  
2. **Сгруппировать** строки по **`ИНН`** (или по однозначному ключу целевой базы).  
3. Для каждой группы сформировать **отдельный HTTP-запрос** (пакет JSON только по строкам этой базы) на URL, полученный из регистра.  
4. Объединить ответы и применить п. 6 к каждой строке по её `registryKey`.

*Плюсы:* нет лишних вызовов «в пустую», проще таймауты и диагностика по базе. *Особенность:* HTTP **без состояния** — отдельного «соединения» как у COM-подключения нет; «держать открытым» нечего, кроме **кэша параметров** (соответствие ИНН → URL, заголовки аутентификации) в памяти на время выполнения задания.

**Вариант B — кэш параметров + опциональный прогрев доступности:**

1. В начале выполнения регламентного задания (или перед первой отправкой в рамках ИНН) по **уникальным ИНН** из текущей выборки загрузить из регистра параметры и положить в **соответствие** `ИНН → параметры HTTP` (и при необходимости предразобранный URL).  
2. Опционально для каждого уникального URL выполнить **лёгкий запрос** (например, существующий `PingMe` сервиса `ИНК_Бухгалтерия`) для раннего обнаружения недоступности базы.  
3. Далее — как вариант A: группировка и пакетные POST по базам.

*Плюсы:* меньше повторных чтений регистра при большом числе строк на один ИНН; ранний сигнал о «мёртвой» базе.

**Вариант C — «прогрев всех баз из регистра»:**

1. При старте задания прочитать **все** записи `ИНК_ПараметрыПодключенийКБазамДанных`, для каждой валидной строки с заполненным HTTP выполнить опциональный Ping (или только заполнить кэш).  
2. Обрабатывать выборку распределений с использованием этого кэша.

*Плюсы:* актуальность проверки всех баз независимо от текущей выборки. *Минусы:* лишняя нагрузка и время, если в выборке одна организация; при большом числе записей в регистре — избыточно.

**Рекомендация ТЗ:** реализовать **вариант A** как основной; кэш из **варианта B** — обязательно внутри одного запуска задания для снижения обращений к регистру; **вариант C** — только по флагу настройки («проверять все базы при старте») при требовании эксплуатации.

**Сортировка и отбор «по организации»:** отбор строк к выгрузке по п. 3.2 остаётся прежним; **фильтрация по одной организации** допускается только в **ручном** сценарии (п. 7.2) для целенаправленной выгрузки в одну БП. Регламентное задание обрабатывает **все** отобранные строки, маршрутизируя их по группам ИНН.

### 3.6. Узел плана обмена УФ: значение для передачи в БП

Для отбора записей регистра **`ПубличныеИдентификаторыСинхронизируемыхОбъектов`** в БП по измерению **`УзелИнформационнойБазы`** сервису БП нужна однозначная ссылка на узел. Она задаётся через строку **`Код`**, согласованную между базами при настройке **синхронизации данных через универсальный формат**.

**В ERP:**

1. Использовать план обмена **`СинхронизацияДанныхЧерезУниверсальныйФормат`** (при расхождении имени с фактическими метаданными внедрения — зафиксировать в коде константу/параметр с полным именем объекта).  
2. Найти **единственный** узел этого плана, для которого **истинен признак «этот узел»** (текущая ИБ — ERP): в пользовательской формулировке — реквизит **`ЭтотУзел = Истина`**; в типовых конфигурациях допускается реализация через одноимённый **метод** узла — ориентироваться на фактическую модель ERP.  
3. Взять у этого узла **`Код`** (строка, в настройке содержит **УИД** в строковом виде).  
4. Эту строку передавать в каждом HTTP-запросе к БП в поле **`exchangeNodeUid`** (см. п. 5.3) — **один раз на запрос** на корневом уровне JSON вместе с массивом строк распределения.

**Кэширование:** значение **`exchangeNodeUid`** для данной ИБ ERP **не зависит** от организации и ИНН; его можно вычислить **один раз за сеанс** регламентного задания (или при первом вызове) и переиспользовать для всех групп по базам БП.

**Если узел «этот узел» не найден или `Код` пустой:** не выполнять вызовы БП для всех строк текущего запуска (или проставлять всем строкам статус `CONFIG_ERROR` на стороне ERP) — требуется исправление настройки плана обмена.

---

## 4. Действия в БП

### 4.1. Поиск документов по УИД

Использовать регистр сведений **`ПубличныеИдентификаторыСинхронизируемыхОбъектов`** (`http/metadata_bp.md`):

- Измерение `Идентификатор` — строка (УИД из ERP).  
- Измерение `Ссылка` — ссылка на объект БП.  
- Измерение `УзелИнформационнойБазы` — узел плана обмена.  
- Реквизит `ТипМетаданных` — строка.  

**Определение `УзелИнформационнойБазы` в БП (закрывает бывший открытый вопрос 1):**

1. Из тела HTTP-запроса взять строку **`exchangeNodeUid`** (см. п. 5.3) — это **`Код`** узла ERP с **`ЭтотУзел = Истина`**, переданный из ERP (п. 3.6).  
2. В плане обмена **`СинхронизацияДанныхЧерезУниверсальныйФормат`** найти узел, у которого **`Код`** совпадает с **`exchangeNodeUid`** и **ложен признак «этот узел»** (в БП этот узел представляет **базу ERP**).  
3. Полученную **ссылку на узел** использовать при отборе регистра: **`УзелИнформационнойБазы = &Узел`**.  

Если узел не найден или найдено более одного узла при отборе по `Код` — для всех строк пакета вернуть ошибку уровня сервиса (`VALIDATION_ERROR` / отдельный код) с пояснением; записи документов не изменять.

**Для каждой строки распределения:**

1. По УИД документа оплаты ERP и найденному **`УзелИнформационнойБазы`** найти ссылку на **`ПоступлениеНаРасчетныйСчет`** в БП.  
2. По УИД документа реализации ERP и тому же узлу найти ссылку на **`РеализацияТоваровУслуг`** в БП.  

**Открытый вопрос 1:** нужно ли дополнительно фильтровать по **`ТипМетаданных`** и каково ожидаемое значение для оплаты и реализации (строка из обмена).

### 4.2. Соответствие типа операции ERP и табличных частей БП

По смыслу задачи (из идеи ТЗ):

| `ТипОперации` (ERP) | Документ, который изменяется в БП | Смысл связи |
|---------------------|-----------------------------------|-------------|
| `Аванс` | `ПоступлениеНаРасчетныйСчет` | В расшифровке платежа указывается **документ расчётов** = реализация (погашение/привязка к сделке — по правилам типовой БП для выбранного способа погашения). |
| `ЗачетОплаты` | `РеализацияТоваровУслуг` | В табличной части **`ЗачетАвансов`** добавляется (или актуализируется) строка: **`ДокументАванса`** = поступление на р/с, **`СуммаЗачета`** = сумма из строки распределения. |

**Ссылка на метаданные БП:**

- `ПоступлениеНаРасчетныйСчет` — табличная часть **`РасшифровкаПлатежа`**: реквизиты в т.ч. `Сделка` (документ расчётов), `СуммаПлатежа`, `СпособПогашенияЗадолженности`, `ДоговорКонтрагента` и др.  
- `РеализацияТоваровУслуг` — табличная часть **`ЗачетАвансов`**: `ДокументАванса`, `СуммаЗачета` (см. `metadata_bp.md`).

**Открытый вопрос 2:** для варианта `Аванс` подтвердить штатный сценарий типовой БП:

- заполняется ли **`РасшифровкаПлатежа.Сделка`** ссылкой на **`РеализацияТоваровУслуг`** и какой **`СпособПогошенияЗадолженности`** должен использоваться;  
- или аванс в БП отражается иначе (отдельная операция/вид оплаты), и требуется ссылка на процедуру из типовой конфигурации.

### 4.3. Правила записи документов

1. Оба документа в БП должны быть **найдены**. Иначе строка **не обрабатывается**, в ответе — код/текст причины, в ERP `ВыгруженоВБП` **не** устанавливается.  
2. Документы не должны быть **помечены на удаление** (иначе — отказ с диагностикой).  
3. **Блокировка от изменений** (закрытый период, запрет редактирования, блокировка сеансом и т.п.): запись **не выполняется**, возвращается информативное сообщение, в ERP `ВыгруженоВБП` остаётся `Ложь`.  
4. После изменения табличной части документы подлежат **проведению** (если были проведены) в порядке, исключающем нарушение учётных связей (уточнить: сначала Поступление, затем Реализация или наоборот — **открытый вопрос 3** в соответствии с типовой логикой зачёта).  
5. **Идемпотентность:** повторная отправка той же строки (тот же состав ключа измерений и та же сумма) не должна приводить к задвоению сумм в БП. Варианты реализации (выбрать при разработке однозначно):  
   - поиск существующей строки ТЧ с тем же «партнёром» связи (ПДС↔РТУ) и той же суммой;  
   - или хранение в ERP/БП служебного ключа строки обмена (расширение — только по согласованию).  

**Открытый вопрос 4:** допускается ли **корректировка суммы** уже выгруженной строки (повтор с другой суммой) — если да, описать правило перезаписи в БП и сброс `ВыгруженоВБП` в ERP.

### 4.4. Валюта и сумма

- В первой версии обмена передаётся **`Сумма`** из регистра ERP **как есть** (валюта взаиморасчётов считается согласованной между парами документов обмена).  
- **Открытый вопрос 5:** если валюты/курсы в паре документов БП расходятся с ERP — отказать с диагностикой или пересчитывать по курсу документа БП.

---

## 5. Транспорт: HTTP-сервис

### 5.1. Размещение кода

- **БП:** расширить HTTP-сервис `ИНК_Бухгалтерия` (`RootURL`: `bp`, см. `ИНК_Бухгалтерия.xml`) новым шаблоном URL и обработчиком; модуль — по аналогии с `http/http_module.bsl` (кодировка UTF-8, формирование JSON-ответа).  
- **ERP:** общий модуль / обработка регламентного задания:  
  - определение целевого URL по п. **3.4** (регистр **`ИНК_ПараметрыПодключенийКБазамДанных`**, сопоставление по **ИНН организации** документа **`ЗаказКлиента`**);  
  - получение **`exchangeNodeUid`** по п. **3.6** (узел УФ-обмена «этот узел» в ERP, поле **`Код`**);  
  - при нескольких организациях — **несколько вызовов** HTTP на разные базы согласно п. **3.5** (пакет запроса не смешивает строки разных ИНН; **`exchangeNodeUid` один и тот же** для всех вызовов из данной ИБ ERP);  
  - сборка вызова — по п. **5.6** (единый стиль с `ПримерСоединенияСБП.md`).

### 5.2. Рекомендуемый режим обмена: пакетный

**Сравнение (п. 5 идеи ТЗ):**

| Подход | Плюсы | Минусы |
|--------|--------|--------|
| Вызов по одной строке | Проще отладка, меньше таймаутов на один запрос | Больше HTTP-накладных расходов, больше round-trip |
| **Пакет (рекомендуется)** | Один round-trip, один сеанс на стороне БП, проще единая транзакционная политика | Больше тело запроса/ответа; нужны лимиты размера пакета и таймаут |

**Рекомендация ТЗ:** один метод **POST** (или GET с телом не используется — предпочтительно **POST**) принимает **массив строк** и возвращает **массив результатов** по каждой строке. Опционально — параметр `?batchSize` на стороне ERP при нарезке больших выборок **внутри одной базы БП** (после группировки по ИНН, п. 3.5).

### 5.3. Формат запроса (проект)

Кодировка: UTF-8, `Content-Type: application/json`.

Тело запроса — **объект** с двумя свойствами верхнего уровня:

| Поле | Тип | Обязательность | Описание |
|------|-----|----------------|----------|
| `exchangeNodeUid` | Строка | Да | **`Код`** узла плана **`СинхронизацияДанныхЧерезУниверсальныйФормат`** в ERP для узла с **«этот узел» = Истина** (п. 3.6). В БП по этому значению находится узел с тем же **`Код`** и **«этот узел» = Ложь** для отбора `ПубличныеИдентификаторыСинхронизируемыхОбъектов` (п. 4.1). |
| `items` | Массив | Да | Массив строк распределения (см. ниже). |

Элементы массива **`items`** — объекты:

| Поле | Тип | Обязательность | Описание |
|------|-----|----------------|----------|
| `paymentUid` | Строка | Да | УИД `ДокументОплаты` (ERP) |
| `realizationUid` | Строка | Да | УИД `Реализация` (ERP) |
| `stageUid` | Строка | Да | `УИДЭтапа` (для трассировки и логов) |
| `fixedFlag` | Булево | Да | Всегда `true` (контроль целостности) |
| `operationType` | Строка | Да | `Аванс` или `ЗачетОплаты` (имя значения перечисления ERP) |
| `amount` | Число | Да | `Сумма` |
| `isExcess` | Булево | Нет | `ЭтоПереизбыток` — для фильтрации/логирования |
| `registryKey` | Объект | Да* | Состав ключа записи регистра ERP для однозначной идемпотентности и ответа (*структура полей — см. п. 5.5) |

**Объект `registryKey` (проект):**

- `order` — УИД заказа клиента (строка)  
- `payment` — УИД оплаты  
- `realization` — УИД реализации  
- `stageUid` — строка  
- `fixed` — булево (`true`)  

Так ERP сможет сопоставить ответ с набором записей регистра без двусмысленности.

### 5.4. Формат ответа (проект)

HTTP 200 при успешной **обработке запроса как целого** (парсинг прошёл), даже если часть строк завершилась ошибкой.

Тело JSON — объект:

- `success` — Булево: `Истина`, если все строки обработаны успешно.  
- `processedAt` — Дата/строка ISO — время на сервере БП.  
- `items` — массив результатов **в том же порядке**, что и в запросе (обязательно закрепить в реализации).

Элемент `items[]`:

| Поле | Тип | Описание |
|------|-----|----------|
| `status` | Строка | `OK` \| `NOT_FOUND` \| `LOCKED` \| `VALIDATION_ERROR` \| `WRITE_ERROR` \| `DUPLICATE` \| `CONFIG_ERROR` (на стороне ERP: нет маршрута по ИНН, нет `exchangeNodeUid` и т.п.; из БП: неверный/пустой `exchangeNodeUid`, узел не найден — по правилам п. 4.1) |
| `message` | Строка | Текст для журнала/администратора |
| `registryKey` | Объект | Эхо ключа из запроса |

При критической ошибке сервиса (500) ERP **не** меняет `ВыгруженоВБП`; предусмотреть повтор регламентного задания.

### 5.5. Безопасность

- Аутентификация вызова HTTP (basic, token в заголовке, IP whitelist) — **открытый вопрос 6**, зафиксировать в настройках ERP/веб-сервера.  
- Выполнение в привилегированном режиме на стороне БП только в границах обработчика и только при необходимости (как в текущем `SalaryStatement`); не расширять права без аудита.

### 5.6. Эталон HTTP-вызова из ERP (существующая практика)

Ниже зафиксирован подход из **`ПримерСоединенияСБП.md`**; реализацию обмена распределениями следует выдерживать **в том же духе** (с заменой метода, глагола HTTP и разбора тела ответа под п. 5.3–5.4).

1. **Контекст выполнения:** серверный код без контекста формы при возможности (`&НаСервереБезКонтекста`); чтение регистра параметров и выполнение HTTP — при необходимости в **`УстановитьПривилегированныйРежим(Истина)`** в границах процедуры, с гарантированным сбросом (как в примере).  
2. **Получение параметров:** запрос к **`РегистрСведений.ИНК_ПараметрыПодключенийКБазамДанных`** с **`ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Организации ПО регистр.ИНН = Организации.ИНН`** и отбором **`Организации.Ссылка = &Организация`**.  
3. **Соединение:** `Новый HTTPСоединение(СерверHTTP, <Порт>)`. В примере порт **`80`** задан явно; для **HTTPS** потребуется порт **`443`** и использование защищённого соединения (параметры конструктора/дополнительные свойства — по версии платформы и политике инфраструктуры, **открытый вопрос 11**).  
4. **Путь:** `Новый HTTPЗапрос(ПодключениеHTTP + "/" + <ИмяШаблонаURL> + <QueryПриНеобходимости>)`. Для текущего сервиса ведомостей в примере: шаблон **`SalaryStatement`**, параметры в query (`datefrom=…`). Для обмена распределениями: шаблон — **имя нового URL-template** в HTTP-сервисе БП (согласовать с `RootURL` публикации, например префикс `bp` в `ИНК_Бухгалтерия.xml`).  
5. **Метод:** в примере — **`Получить`** (GET). Для тела JSON по п. 5.2 — **`ОтправитьДляОбработки`** (POST) или эквивалент с установкой тела и заголовка `Content-Type: application/json; charset=utf-8`.  
6. **Ответ:** проверка **`Ответ.КодСостояния`** (в примере ожидается `200`). Разбор тела: в примере для ведомостей — **`СериализаторXDTO.ПрочитатьJSON`** в `ТаблицаЗначений`; для нового контракта — **`ЧтениеJSON` + `ПрочитатьJSON`** в структуру/соответствие по согласованной схеме **или** XDTO, **без** смешения с форматом `SalaryStatement`.  
7. **Ошибки без HTTP:** если `ПодключениеHTTP = ""` или `СерверHTTP = ""` — не вызывать соединение; вернуть диагностику на уровне ERP (`CONFIG_ERROR`).

---

## 6. Действия в ERP после ответа БП

Для **каждой** строки запроса, для которой в `items[]` пришёл `status = OK`:

1. Выполнить запись в регистр `ИНК_РаспределениеОплатПоЭтапам` с установкой реквизита **`ВыгруженоВБП = Истина`** для записи с соответствующим ключом измерений (без изменения `Зафиксировано` и ресурсов распределения).  
2. Обновить реквизиты диагностики обмена по п. **3.7** (`OK`, дата/время, при необходимости очистка сообщения).

Для строк с любым иным статусом:

- **`ВыгруженоВБП` не менять** (`Ложь`).  
- Записать в ту же строку регистра реквизиты п. **3.7** (код статуса, `message`, дата/время).  
- Дополнительно дублировать сведения в **журнал регистрации** / **`ИНК_ДиагностикаРаспределенияОплат`** — по решению внедрения (не обязательно, если достаточно полей регистра).

**Транзакция ERP:** целесообразно обновлять флаги и реквизиты диагностики **после** успешного разбора ответа; пакетное обновление наборами записей по ключам строк.

---

## 7. Регламентное задание и ручной запуск

### 7.1. Регламентное задание (ERP)

- Периодичность: согласовать (например, каждые 15–60 минут в рабочее время).  
- Действия: отбор по п. 3.2 → получение **`exchangeNodeUid`** (п. 3.6, один раз на запуск) → получение ИНН по `ЗаказКлиента` (пакетно) → **группировка строк по целевой базе БП** (п. 3.4–3.5) → для каждой группы: нарезка пакетов (лимит N строк) → вызов HTTP на URL этой базы с телом `{ exchangeNodeUid, items }` → обработка ответа по п. 6.  
- При ошибке сети/таймаута по конкретной базе: для затронутых строк флаги не менять; остальные группы (другие ИНН) обработать в том же запуске, если это реализовано без общей откатывающей транзакции на весь сеанс.  
- Кэш «ИНН → URL/заголовки» в пределах одного запуска задания — по п. 3.5 (вариант B).

### 7.2. Ручной запуск

- Команда/обработка в ERP: «Выгрузить распределения в БП» с отбором (по заказу, по периоду оплат, по конкретным строкам — **открытый вопрос 8**).  
- Использовать тот же программный интерфейс, что и регламентное задание.

---

## 8. Нефункциональные требования

- **Производительность:** целевой размер пакета по умолчанию 50–200 строк **на одну базу БП** (настраиваемо); при превышении времени ответа — уменьшать пакет. Учитывать, что N групп по ИНН даёт до N последовательных (или ограниченно параллельных — не входит в ТЗ) циклов вызова.  
- **Журналирование:** фиксировать длительность вызова **по каждому URL/ИНН**, количество строк в пакете, число `OK` / ошибок, обезличенный идентификатор пакета (GUID сеанса обмена), при необходимости — `ИмяБазы` из регистра параметров.  
- **Повторный запуск:** безопасен за счёт идемпотентности (п. 4.3).

---

## 9. Тестирование (приёмочные сценарии)

1. Оба документа в БП есть, документы не заблокированы, `ТипОперации = ЗачетОплаты` → в РТУ появляется/обновляется зачёт, ERP `ВыгруженоВБП = Истина`.  
2. То же для `Аванс` → корректное заполнение расшифровки Поступления (после уточнения п. 4.2).  
3. Отсутствует оплата в `ПубличныеИдентификаторы...` → `NOT_FOUND`, флаг в ERP не меняется.  
4. Отсутствует реализация в регистре → `NOT_FOUND`.  
5. Документ заблокирован → `LOCKED`.  
6. Повторная выгрузка той же строки → не удваивает суммы в БП, `OK` или `DUPLICATE` с явной семантикой.  
7. Пакет из 3 строк: 1-я `OK`, 2-я ошибка, 3-я `OK` → в ERP обновляются только 1 и 3.  
8. Две строки распределения с заказами **разных организаций** (разные ИНН) → два HTTP-запроса на разные URL из регистра `ИНК_ПараметрыПодключенийКБазамДанных`, флаги обновляются независимо по результатам каждого ответа.  
9. ИНН организации заказа **отсутствует** в регистре параметров → строка не вызывает БП, диагностика маршрутизации, `ВыгруженоВБП` не меняется.  
10. Неверный или неизвестный **`exchangeNodeUid`** в БП → все строки пакета с ошибкой (`VALIDATION_ERROR`), документы не меняются.  
11. Корректный **`exchangeNodeUid`**, документы найдены по узлу → успешная обработка (как в сценарии 1).

---

## 10. Открытые вопросы (сводка)

| № | Вопрос |
|---|--------|
| 1 | Использование и значения `ТипМетаданных` |
| 2 | Точная схема отражения `Аванс` в `ПоступлениеНаРасчетныйСчет` (ТЧ, способ погашения) |
| 3 | Порядок проведения документов БП после изменения |
| 4 | Политика изменения суммы уже выгруженной строки |
| 5 | Валюты и расхождения сумм |
| 6 | Метод аутентификации HTTP |
| 7 | Нужен ли дубль диагностики в журнале регистрации / `ИНК_ДиагностикаРаспределенияОплат` при наличии п. 3.7 |
| 8 | Отборы для ручной выгрузки |
| 9 | Несколько записей в `ИНК_ПараметрыПодключенийКБазамДанных` с одним `ИНН` — правило выбора |
| 10 | Учётные данные / доп. заголовки HTTP, если появятся в регистре или внешнем хранилище |
| 11 | HTTPS: порт, параметры `HTTPСоединение` (защищённое соединение), сертификаты |
| 12 | Если в ERP несколько планов УФ или переименование объекта — нужна ли настройка имени плана/отбор узла не только по «этот узел» |

---

## 11. История изменений

| Версия | Дата | Изменения |
|--------|------|-----------|
| 1.0 | 06.04.2026 | Первоначальная версия по `Идея ТЗ по обмену.md` и метаданным репозитория |
| 1.1 | 06.04.2026 | Регистр `ИНК_ПараметрыПодключенийКБазамДанных`, маршрутизация по ИНН организации из `ЗаказКлиента`; стратегии группировки и кэша/прогрева для нескольких баз БП |
| 1.2 | 06.04.2026 | Ссылка на `ПримерСоединенияСБП.md`; п. 5.6 — зафиксированный паттерн `HTTPСоединение` + `ПодключениеHTTP`/`СерверHTTP`; уточнены роли полей регистра |
| 1.3 | 06.04.2026 | П. 3.6 и `exchangeNodeUid`: узел УФ `СинхронизацияДанныхЧерезУниверсальныйФормат` (`Код`, «этот узел» в ERP / не «этот узел» в БП); п. 4.1 и JSON `{ exchangeNodeUid, items }`; нумерация открытых вопросов |
| 1.4 | 06.04.2026 | П. 1.3: первичная синхронизация не в скоупе; уточнено отношение к алгоритму распределения (v7) |
| 1.5 | 06.04.2026 | П. 1.3: обмен не связан с распределением; убрана ссылка на v7 из материалов; п. 3.7 и п. 6 — диагностика обмена реквизитами в том же регистре |
